{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y ffmpeg libsndfile1"
  },
  "deploy": {
    "startCommand": "python whisper_api.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
